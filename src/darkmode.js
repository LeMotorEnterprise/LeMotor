import React, { useState, useEffect } from "react"; function DarkMode() { const [darkMode, setDarkMode] = useState(false); const buttonStyle = { position: "fixed", bottom: "90px", right: "30px", zIndex: "99", fontSize: "18px", outline: "none", backgroundColor: "#051d40", color: "white", cursor: "pointer", padding: "15px", borderRadius: "4px", border: "1px solid white", }; useEffect(() => { document.body.style.backgroundColor = darkMode ? "#202833" : "white"; document.body.style.color = darkMode ? "white" : "#202833"; const h1Tags = document.querySelectorAll("h1"); h1Tags.forEach((h1) => { h1.style.color = darkMode ? "white" : "#202833"; }); const topBannerContainer = document.getElementsByClassName("topbannerContainer")[0]; if (topBannerContainer) { topBannerContainer.style.backgroundColor = darkMode ? "black" : "white"; } const topBanner = document.getElementsByClassName("topbanner")[0]; if (topBanner) { topBanner.style.filter = darkMode ? "invert(1)" : "invert(0)"; } const carmartWrapper = document.getElementsByClassName("carmart-wrapper")[0]; if (carmartWrapper) { carmartWrapper.style.color = "white"; } const splineContainer = document.getElementsByClassName("splineContainer")[0]; if (splineContainer) { splineContainer.style.display = darkMode ? "none" : "initial"; } const videoContainer = document.getElementsByClassName("videoContainer")[0]; if (videoContainer) { videoContainer.style.display = darkMode ? "initial" : "none"; } const grid1Maps = document.getElementsByClassName("grid1maps")[0]; if (grid1Maps) { grid1Maps.style.filter = darkMode ? "invert(1)" : "invert(0)"; } const footerStyle = document.getElementsByClassName("footerStyle")[0]; if (footerStyle) { footerStyle.style.filter = darkMode ? "invert(1)" : "invert(0)"; footerStyle.style.paddingTop = darkMode ? "7%" : "0%"; } const ModeButton = document.getElementsByClassName("ModeButton")[0]; if (ModeButton) { ModeButton.style.backgroundColor = darkMode ? "white" : "#051d40"; ModeButton.style.color = darkMode ? "#051d40" : "white"; } const socialsContainer = document.getElementsByClassName("socials-container")[0]; if (socialsContainer) { socialsContainer.style.color = darkMode ? "white" : "black"; } }, [darkMode]); return ( <div> <button style={buttonStyle} className="ModeButton" onClick={() => { setDarkMode(!darkMode); }} > {} {darkMode ? "Toggle Light Mode" : "Toggle Dark Mode"} </button> </div> ); } export default DarkMode; 